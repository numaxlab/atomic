# @numaxlab/atomic

## Build Setup

```bash
# install dependencies
$ yarn install

# compile for development
$ yarn dev

# compile for development with watch
$ yarn watch

# compile for production
$ yarn build
```
